public static class VictoryData
{
    public static bool isBall;          // true = BallRunner, false = Freeway
    public static int retryIndex;       // escena a reintentar
    public static int nextIndex;        // siguiente nivel (o -1 si no hay)
}
